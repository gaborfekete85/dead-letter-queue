apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dlt-service.fullname" . }}-test-connection"
  labels:
    {{- include "dlt-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dlt-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
